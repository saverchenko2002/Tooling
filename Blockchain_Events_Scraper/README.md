# setup for windows

```
python -m venv .venv
.venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

# setup for linux

```
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

# execution

```
touch .env
python -m main
```
